<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/question_nav.xml"
    app:startDestination="@id/questionFormFragment">

    <fragment
        android:id="@+id/cameraFragment"
        android:name="org.softwaremaestro.presenter.question_upload.CameraFragment"
        android:label="fragment_camera"
        tools:layout="@layout/fragment_camera">
        <action
            android:id="@+id/action_cameraToDetail"
            app:destination="@id/cameraFragment" />
    </fragment>
    <fragment
        android:id="@+id/questionFormFragment"
        android:name="org.softwaremaestro.presenter.question_upload.QuestionFormFragment"
        android:label="fragment_question_form"
        tools:layout="@layout/fragment_question_form" />

</navigation>